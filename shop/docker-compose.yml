version: "3.7"

services:

  shopware:
    image: dockware/essentials:latest
    container_name: shop
    ports:
      - "80:80"
      - "22:22"
      - "3306:3306"
    volumes:
      - "./src:/var/www/html"
      # excludes
      - '/var/www/html/vendor'
      - '/var/www/html/var/cache'

volumes:
  db_volume:
    driver: local
